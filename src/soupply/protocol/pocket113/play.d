/*
 * This file has been automatically generated by Soupply and released under the MIT license.
 * Generated from pocket113
 */
module soupply.protocol.pocket113.play;

static import std.conv;
import std.system : Endian;
import std.typetuple : TypeTuple;
import packetmaker;

import soupply.util : Tuple, UUID;
import soupply.metadata.pocket113;
import soupply.protocol.pocket113.packet : Pocket113Packet;

static import soupply.protocol.pocket113.types;

alias Packets = TypeTuple!(Login, PlayStatus, ServerToClientHandshake, ClientToServerHandshake, Disconnect, ResourcePacksInfo, ResourcePacksStackPacket, ResourcePackClientResponse, Text, SetTime, StartGame, AddPlayer, AddEntity, RemoveEntity, AddItemEntity, AddHangingEntity, TakeItemEntity, MoveEntity, MovePlayer, RiderJump, RemoveBlock, UpdateBlock, AddPainting, Explode, LevelSoundEvent, LevelEvent, BlockEvent, EntityEvent, MobEffect, UpdateAttributes, MobEquipment, MobArmorEquipment, Interact, BlockPickRequest, UseItem, PlayerAction, EntityFall, HurtArmor, SetEntityData, SetEntityMotion, SetEntityLink, SetHealth, SetSpawnPosition, Animate, Respawn, DropItem, InventoryAction, ContainerOpen, ContainerClose, ContainerSetSlot, ContainerSetData, ContainerSetContent, CraftingData, CraftingEvent, AdventureSettings, BlockEntityData, PlayerInput, FullChunkData, SetCommandsEnabled, SetDifficulty, ChangeDimension, SetPlayerGameType, PlayerList, SimpleEvent, TelemetryEvent, SpawnExperienceOrb, ClientboundMapItemData, MapInfoRequest, RequestChunkRadius, ChunkRadiusUpdated, ItemFrameDropItem, ReplaceItemInSlot, GameRulesChanged, Camera, AddItem, BossEvent, ShowCredits, AvailableCommands, CommandStep, CommandBlockUpdate, UpdateTrade, UpdateEquip, ResourcePackDataInfo, ResourcePackChunkData, ResourcePackChunkRequest, Transfer, PlaySound, StopSound, SetTitle, AddBehaviorTree, StructureBlockUpdate, ShowStoreOffer, PurchaseReceipt);

class Login : Pocket113Packet
{

    enum ubyte ID = 1;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    // version
    enum ubyte VANILLA = 0;
    enum ubyte EDUCATION = 1;

    enum string[] __fields = ["protocol", "version_", "body_"];

    uint protocol = 113;
    ubyte version_;
    soupply.protocol.pocket113.types.LoginBody body_;

    this() pure nothrow @safe @nogc {}

    this(uint protocol, ubyte version_=ubyte.init, soupply.protocol.pocket113.types.LoginBody body_=soupply.protocol.pocket113.types.LoginBody.init) pure nothrow @safe @nogc
    {
        this.protocol = protocol;
        this.version_ = version_;
        this.body_ = body_;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        Login ret = new Login();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "Login(protocol: " ~ std.conv.to!string(this.protocol) ~ ", version_: " ~ std.conv.to!string(this.version_) ~ ", body_: " ~ std.conv.to!string(this.body_) ~ ")";
    }

}

class PlayStatus : Pocket113Packet
{

    enum ubyte ID = 2;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    // status
    enum uint OK = 0;
    enum uint OUTDATED_CLIENT = 1;
    enum uint OUTDATED_SERVER = 2;
    enum uint SPAWNED = 3;
    enum uint INVALID_TENANT = 4;
    enum uint EDITION_MISMATCH_EDU_TO_VANILLA = 5;
    enum uint EDITION_MISMATCH_VANILLA_TO_EDU = 6;

    enum string[] __fields = ["status"];

    uint status;

    this() pure nothrow @safe @nogc {}

    this(uint status) pure nothrow @safe @nogc
    {
        this.status = status;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        PlayStatus ret = new PlayStatus();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "PlayStatus(status: " ~ std.conv.to!string(this.status) ~ ")";
    }

}

class ServerToClientHandshake : Pocket113Packet
{

    enum ubyte ID = 3;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["serverPublicKey", "token"];

    string serverPublicKey;
    ubyte[] token;

    this() pure nothrow @safe @nogc {}

    this(string serverPublicKey, ubyte[] token=(ubyte[]).init) pure nothrow @safe @nogc
    {
        this.serverPublicKey = serverPublicKey;
        this.token = token;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ServerToClientHandshake ret = new ServerToClientHandshake();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ServerToClientHandshake(serverPublicKey: " ~ std.conv.to!string(this.serverPublicKey) ~ ", token: " ~ std.conv.to!string(this.token) ~ ")";
    }

}

class ClientToServerHandshake : Pocket113Packet
{

    enum ubyte ID = 4;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = [];

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ClientToServerHandshake ret = new ClientToServerHandshake();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ClientToServerHandshake()";
    }

}

class Disconnect : Pocket113Packet
{

    enum ubyte ID = 5;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["hideDisconnectionScreen", "message"];

    bool hideDisconnectionScreen;
    @Condition("hideDisconnectionScreen==false") string message;

    this() pure nothrow @safe @nogc {}

    this(bool hideDisconnectionScreen, string message=string.init) pure nothrow @safe @nogc
    {
        this.hideDisconnectionScreen = hideDisconnectionScreen;
        this.message = message;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        Disconnect ret = new Disconnect();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "Disconnect(hideDisconnectionScreen: " ~ std.conv.to!string(this.hideDisconnectionScreen) ~ ", message: " ~ std.conv.to!string(this.message) ~ ")";
    }

}

class ResourcePacksInfo : Pocket113Packet
{

    enum ubyte ID = 6;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["mustAccept", "behaviourPacks", "resourcePacks"];

    bool mustAccept;
    @EndianLength!ushort(Endian.littleEndian) soupply.protocol.pocket113.types.PackWithSize[] behaviourPacks;
    @EndianLength!ushort(Endian.littleEndian) soupply.protocol.pocket113.types.PackWithSize[] resourcePacks;

    this() pure nothrow @safe @nogc {}

    this(bool mustAccept, soupply.protocol.pocket113.types.PackWithSize[] behaviourPacks=(soupply.protocol.pocket113.types.PackWithSize[]).init, soupply.protocol.pocket113.types.PackWithSize[] resourcePacks=(soupply.protocol.pocket113.types.PackWithSize[]).init) pure nothrow @safe @nogc
    {
        this.mustAccept = mustAccept;
        this.behaviourPacks = behaviourPacks;
        this.resourcePacks = resourcePacks;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ResourcePacksInfo ret = new ResourcePacksInfo();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ResourcePacksInfo(mustAccept: " ~ std.conv.to!string(this.mustAccept) ~ ", behaviourPacks: " ~ std.conv.to!string(this.behaviourPacks) ~ ", resourcePacks: " ~ std.conv.to!string(this.resourcePacks) ~ ")";
    }

}

class ResourcePacksStackPacket : Pocket113Packet
{

    enum ubyte ID = 7;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["mustAccept", "behaviourPacks", "resourcePacks"];

    bool mustAccept;
    soupply.protocol.pocket113.types.Pack[] behaviourPacks;
    soupply.protocol.pocket113.types.Pack[] resourcePacks;

    this() pure nothrow @safe @nogc {}

    this(bool mustAccept, soupply.protocol.pocket113.types.Pack[] behaviourPacks=(soupply.protocol.pocket113.types.Pack[]).init, soupply.protocol.pocket113.types.Pack[] resourcePacks=(soupply.protocol.pocket113.types.Pack[]).init) pure nothrow @safe @nogc
    {
        this.mustAccept = mustAccept;
        this.behaviourPacks = behaviourPacks;
        this.resourcePacks = resourcePacks;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ResourcePacksStackPacket ret = new ResourcePacksStackPacket();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ResourcePacksStackPacket(mustAccept: " ~ std.conv.to!string(this.mustAccept) ~ ", behaviourPacks: " ~ std.conv.to!string(this.behaviourPacks) ~ ", resourcePacks: " ~ std.conv.to!string(this.resourcePacks) ~ ")";
    }

}

class ResourcePackClientResponse : Pocket113Packet
{

    enum ubyte ID = 8;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    // status
    enum ubyte REFUSED = 1;
    enum ubyte SEND_PACKS = 2;
    enum ubyte HAVE_ALL_PACKS = 3;
    enum ubyte COMPLETED = 4;

    enum string[] __fields = ["status", "packIds"];

    ubyte status;
    @EndianLength!ushort(Endian.littleEndian) string[] packIds;

    this() pure nothrow @safe @nogc {}

    this(ubyte status, string[] packIds=(string[]).init) pure nothrow @safe @nogc
    {
        this.status = status;
        this.packIds = packIds;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ResourcePackClientResponse ret = new ResourcePackClientResponse();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ResourcePackClientResponse(status: " ~ std.conv.to!string(this.status) ~ ", packIds: " ~ std.conv.to!string(this.packIds) ~ ")";
    }

}

class Text : Pocket113Packet
{

    enum ubyte ID = 9;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["type"];

    ubyte type;

    this() pure nothrow @safe @nogc {}

    this(ubyte type) pure nothrow @safe @nogc
    {
        this.type = type;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        Text ret = new Text();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "Text(type: " ~ std.conv.to!string(this.type) ~ ")";
    }

    enum string variantField = "type";

    alias Variants = TypeTuple!(Raw, Chat, Translation, Popup, Tip, System, Whisper, Announcement);

    class Raw : Pocket113Packet
    {

        enum typeof(type) TYPE = 0;

        enum string[] __fields = ["message"];

        string message;

        this() pure nothrow @safe @nogc {}

        this(string message) pure nothrow @safe @nogc
        {
            this.message = message;
        }

        mixin MakeNested;

        public override string toString()
        {
            return "Text.Raw(message: " ~ std.conv.to!string(this.message) ~ ")";
        }

    }

    class Chat : Pocket113Packet
    {

        enum typeof(type) TYPE = 1;

        enum string[] __fields = ["sender", "message"];

        string sender;
        string message;

        this() pure nothrow @safe @nogc {}

        this(string sender, string message=string.init) pure nothrow @safe @nogc
        {
            this.sender = sender;
            this.message = message;
        }

        mixin MakeNested;

        public override string toString()
        {
            return "Text.Chat(sender: " ~ std.conv.to!string(this.sender) ~ ", message: " ~ std.conv.to!string(this.message) ~ ")";
        }

    }

    class Translation : Pocket113Packet
    {

        enum typeof(type) TYPE = 2;

        enum string[] __fields = ["message", "parameters"];

        string message;
        string[] parameters;

        this() pure nothrow @safe @nogc {}

        this(string message, string[] parameters=(string[]).init) pure nothrow @safe @nogc
        {
            this.message = message;
            this.parameters = parameters;
        }

        mixin MakeNested;

        public override string toString()
        {
            return "Text.Translation(message: " ~ std.conv.to!string(this.message) ~ ", parameters: " ~ std.conv.to!string(this.parameters) ~ ")";
        }

    }

    class Popup : Pocket113Packet
    {

        enum typeof(type) TYPE = 3;

        enum string[] __fields = ["title", "subtitle"];

        string title;
        string subtitle;

        this() pure nothrow @safe @nogc {}

        this(string title, string subtitle=string.init) pure nothrow @safe @nogc
        {
            this.title = title;
            this.subtitle = subtitle;
        }

        mixin MakeNested;

        public override string toString()
        {
            return "Text.Popup(title: " ~ std.conv.to!string(this.title) ~ ", subtitle: " ~ std.conv.to!string(this.subtitle) ~ ")";
        }

    }

    class Tip : Pocket113Packet
    {

        enum typeof(type) TYPE = 4;

        enum string[] __fields = ["message"];

        string message;

        this() pure nothrow @safe @nogc {}

        this(string message) pure nothrow @safe @nogc
        {
            this.message = message;
        }

        mixin MakeNested;

        public override string toString()
        {
            return "Text.Tip(message: " ~ std.conv.to!string(this.message) ~ ")";
        }

    }

    class System : Pocket113Packet
    {

        enum typeof(type) TYPE = 5;

        enum string[] __fields = ["message"];

        string message;

        this() pure nothrow @safe @nogc {}

        this(string message) pure nothrow @safe @nogc
        {
            this.message = message;
        }

        mixin MakeNested;

        public override string toString()
        {
            return "Text.System(message: " ~ std.conv.to!string(this.message) ~ ")";
        }

    }

    class Whisper : Pocket113Packet
    {

        enum typeof(type) TYPE = 6;

        enum string[] __fields = ["sender", "message"];

        string sender;
        string message;

        this() pure nothrow @safe @nogc {}

        this(string sender, string message=string.init) pure nothrow @safe @nogc
        {
            this.sender = sender;
            this.message = message;
        }

        mixin MakeNested;

        public override string toString()
        {
            return "Text.Whisper(sender: " ~ std.conv.to!string(this.sender) ~ ", message: " ~ std.conv.to!string(this.message) ~ ")";
        }

    }

    class Announcement : Pocket113Packet
    {

        enum typeof(type) TYPE = 7;

        enum string[] __fields = ["announcer", "message"];

        string announcer;
        string message;

        this() pure nothrow @safe @nogc {}

        this(string announcer, string message=string.init) pure nothrow @safe @nogc
        {
            this.announcer = announcer;
            this.message = message;
        }

        mixin MakeNested;

        public override string toString()
        {
            return "Text.Announcement(announcer: " ~ std.conv.to!string(this.announcer) ~ ", message: " ~ std.conv.to!string(this.message) ~ ")";
        }

    }

}

class SetTime : Pocket113Packet
{

    enum ubyte ID = 10;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["time"];

    @Var int time;

    this() pure nothrow @safe @nogc {}

    this(int time) pure nothrow @safe @nogc
    {
        this.time = time;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        SetTime ret = new SetTime();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "SetTime(time: " ~ std.conv.to!string(this.time) ~ ")";
    }

}

class StartGame : Pocket113Packet
{

    enum ubyte ID = 11;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    // gamemode
    enum int SURVIVAL = 0;
    enum int CREATIVE = 1;
    enum int ADVENTURE = 2;

    // dimension
    enum int OVERWORLD = 0;
    enum int NETHER = 1;
    enum int END = 2;

    // generator
    enum int OLD = 0;
    enum int INFINITE = 1;
    enum int FLAT = 2;

    // difficulty
    enum int PEACEFUL = 0;
    enum int EASY = 1;
    enum int NORMAL = 2;
    enum int HARD = 3;

    // version
    enum ubyte VANILLA = 0;
    enum ubyte EDUCATION = 1;

    enum string[] __fields = ["entityId", "runtimeId", "gamemode", "position", "yaw", "pitch", "seed", "dimension", "generator", "worldGamemode", "difficulty", "spawnPosition", "loadedInCreative", "time", "version_", "rainLevel", "lightningLevel", "commandsEnabled", "textureRequired", "gameRules", "levelId", "worldName", "premiumWorldTemplate", "unknown23", "worldTicks"];

    @Var long entityId;
    @Var long runtimeId;
    @Var int gamemode;
    Tuple!(float, "xyz") position;
    float yaw;
    float pitch;
    @Var int seed;
    @Var int dimension = 0;
    @Var int generator = 1;
    @Var int worldGamemode;
    @Var int difficulty;
    @Var Tuple!(int, "xyz") spawnPosition;
    bool loadedInCreative;
    @Var int time;
    ubyte version_;
    float rainLevel;
    float lightningLevel;
    bool commandsEnabled;
    bool textureRequired;
    soupply.protocol.pocket113.types.Rule[] gameRules;
    string levelId;
    string worldName;
    string premiumWorldTemplate;
    bool unknown23;
    @LittleEndian ulong worldTicks;

    this() pure nothrow @safe @nogc {}

    this(long entityId, long runtimeId=long.init, int gamemode=int.init, Tuple!(float, "xyz") position=Tuple!(float, "xyz").init, float yaw=float.init, float pitch=float.init, int seed=int.init, int dimension=0, int generator=1, int worldGamemode=int.init, int difficulty=int.init, Tuple!(int, "xyz") spawnPosition=Tuple!(int, "xyz").init, bool loadedInCreative=bool.init, int time=int.init, ubyte version_=ubyte.init, float rainLevel=float.init, float lightningLevel=float.init, bool commandsEnabled=bool.init, bool textureRequired=bool.init, soupply.protocol.pocket113.types.Rule[] gameRules=(soupply.protocol.pocket113.types.Rule[]).init, string levelId=string.init, string worldName=string.init, string premiumWorldTemplate=string.init, bool unknown23=bool.init, ulong worldTicks=ulong.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.runtimeId = runtimeId;
        this.gamemode = gamemode;
        this.position = position;
        this.yaw = yaw;
        this.pitch = pitch;
        this.seed = seed;
        this.dimension = dimension;
        this.generator = generator;
        this.worldGamemode = worldGamemode;
        this.difficulty = difficulty;
        this.spawnPosition = spawnPosition;
        this.loadedInCreative = loadedInCreative;
        this.time = time;
        this.version_ = version_;
        this.rainLevel = rainLevel;
        this.lightningLevel = lightningLevel;
        this.commandsEnabled = commandsEnabled;
        this.textureRequired = textureRequired;
        this.gameRules = gameRules;
        this.levelId = levelId;
        this.worldName = worldName;
        this.premiumWorldTemplate = premiumWorldTemplate;
        this.unknown23 = unknown23;
        this.worldTicks = worldTicks;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        StartGame ret = new StartGame();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "StartGame(entityId: " ~ std.conv.to!string(this.entityId) ~ ", runtimeId: " ~ std.conv.to!string(this.runtimeId) ~ ", gamemode: " ~ std.conv.to!string(this.gamemode) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", yaw: " ~ std.conv.to!string(this.yaw) ~ ", pitch: " ~ std.conv.to!string(this.pitch) ~ ", seed: " ~ std.conv.to!string(this.seed) ~ ", dimension: " ~ std.conv.to!string(this.dimension) ~ ", generator: " ~ std.conv.to!string(this.generator) ~ ", worldGamemode: " ~ std.conv.to!string(this.worldGamemode) ~ ", difficulty: " ~ std.conv.to!string(this.difficulty) ~ ", spawnPosition: " ~ std.conv.to!string(this.spawnPosition) ~ ", loadedInCreative: " ~ std.conv.to!string(this.loadedInCreative) ~ ", time: " ~ std.conv.to!string(this.time) ~ ", version_: " ~ std.conv.to!string(this.version_) ~ ", rainLevel: " ~ std.conv.to!string(this.rainLevel) ~ ", lightningLevel: " ~ std.conv.to!string(this.lightningLevel) ~ ", commandsEnabled: " ~ std.conv.to!string(this.commandsEnabled) ~ ", textureRequired: " ~ std.conv.to!string(this.textureRequired) ~ ", gameRules: " ~ std.conv.to!string(this.gameRules) ~ ", levelId: " ~ std.conv.to!string(this.levelId) ~ ", worldName: " ~ std.conv.to!string(this.worldName) ~ ", premiumWorldTemplate: " ~ std.conv.to!string(this.premiumWorldTemplate) ~ ", unknown23: " ~ std.conv.to!string(this.unknown23) ~ ", worldTicks: " ~ std.conv.to!string(this.worldTicks) ~ ")";
    }

}

class AddPlayer : Pocket113Packet
{

    enum ubyte ID = 12;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["uuid", "username", "entityId", "runtimeId", "position", "motion", "pitch", "headYaw", "yaw", "heldItem", "metadata"];

    soupply.protocol.pocket113.types.McpeUuid uuid;
    string username;
    @Var long entityId;
    @Var long runtimeId;
    Tuple!(float, "xyz") position;
    Tuple!(float, "xyz") motion;
    float pitch;
    float headYaw;
    float yaw;
    soupply.protocol.pocket113.types.Slot heldItem;
    Metadata metadata;

    this() pure nothrow @safe @nogc {}

    this(soupply.protocol.pocket113.types.McpeUuid uuid, string username=string.init, long entityId=long.init, long runtimeId=long.init, Tuple!(float, "xyz") position=Tuple!(float, "xyz").init, Tuple!(float, "xyz") motion=Tuple!(float, "xyz").init, float pitch=float.init, float headYaw=float.init, float yaw=float.init, soupply.protocol.pocket113.types.Slot heldItem=soupply.protocol.pocket113.types.Slot.init, Metadata metadata=Metadata.init) pure nothrow @safe @nogc
    {
        this.uuid = uuid;
        this.username = username;
        this.entityId = entityId;
        this.runtimeId = runtimeId;
        this.position = position;
        this.motion = motion;
        this.pitch = pitch;
        this.headYaw = headYaw;
        this.yaw = yaw;
        this.heldItem = heldItem;
        this.metadata = metadata;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        AddPlayer ret = new AddPlayer();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "AddPlayer(uuid: " ~ std.conv.to!string(this.uuid) ~ ", username: " ~ std.conv.to!string(this.username) ~ ", entityId: " ~ std.conv.to!string(this.entityId) ~ ", runtimeId: " ~ std.conv.to!string(this.runtimeId) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", motion: " ~ std.conv.to!string(this.motion) ~ ", pitch: " ~ std.conv.to!string(this.pitch) ~ ", headYaw: " ~ std.conv.to!string(this.headYaw) ~ ", yaw: " ~ std.conv.to!string(this.yaw) ~ ", heldItem: " ~ std.conv.to!string(this.heldItem) ~ ", metadata: " ~ std.conv.to!string(this.metadata) ~ ")";
    }

}

class AddEntity : Pocket113Packet
{

    enum ubyte ID = 13;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["entityId", "runtimeId", "type", "position", "motion", "pitch", "yaw", "attributes", "metadata", "links"];

    @Var long entityId;
    @Var long runtimeId;
    @Var uint type;
    Tuple!(float, "xyz") position;
    Tuple!(float, "xyz") motion;
    float pitch;
    float yaw;
    soupply.protocol.pocket113.types.Attribute[] attributes;
    Metadata metadata;
    soupply.protocol.pocket113.types.Link[] links;

    this() pure nothrow @safe @nogc {}

    this(long entityId, long runtimeId=long.init, uint type=uint.init, Tuple!(float, "xyz") position=Tuple!(float, "xyz").init, Tuple!(float, "xyz") motion=Tuple!(float, "xyz").init, float pitch=float.init, float yaw=float.init, soupply.protocol.pocket113.types.Attribute[] attributes=(soupply.protocol.pocket113.types.Attribute[]).init, Metadata metadata=Metadata.init, soupply.protocol.pocket113.types.Link[] links=(soupply.protocol.pocket113.types.Link[]).init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.runtimeId = runtimeId;
        this.type = type;
        this.position = position;
        this.motion = motion;
        this.pitch = pitch;
        this.yaw = yaw;
        this.attributes = attributes;
        this.metadata = metadata;
        this.links = links;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        AddEntity ret = new AddEntity();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "AddEntity(entityId: " ~ std.conv.to!string(this.entityId) ~ ", runtimeId: " ~ std.conv.to!string(this.runtimeId) ~ ", type: " ~ std.conv.to!string(this.type) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", motion: " ~ std.conv.to!string(this.motion) ~ ", pitch: " ~ std.conv.to!string(this.pitch) ~ ", yaw: " ~ std.conv.to!string(this.yaw) ~ ", attributes: " ~ std.conv.to!string(this.attributes) ~ ", metadata: " ~ std.conv.to!string(this.metadata) ~ ", links: " ~ std.conv.to!string(this.links) ~ ")";
    }

}

class RemoveEntity : Pocket113Packet
{

    enum ubyte ID = 14;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["entityId"];

    @Var long entityId;

    this() pure nothrow @safe @nogc {}

    this(long entityId) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        RemoveEntity ret = new RemoveEntity();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "RemoveEntity(entityId: " ~ std.conv.to!string(this.entityId) ~ ")";
    }

}

class AddItemEntity : Pocket113Packet
{

    enum ubyte ID = 15;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["entityId", "runtimeId", "item", "position", "motion", "metadata"];

    @Var long entityId;
    @Var long runtimeId;
    soupply.protocol.pocket113.types.Slot item;
    Tuple!(float, "xyz") position;
    Tuple!(float, "xyz") motion;
    Metadata metadata;

    this() pure nothrow @safe @nogc {}

    this(long entityId, long runtimeId=long.init, soupply.protocol.pocket113.types.Slot item=soupply.protocol.pocket113.types.Slot.init, Tuple!(float, "xyz") position=Tuple!(float, "xyz").init, Tuple!(float, "xyz") motion=Tuple!(float, "xyz").init, Metadata metadata=Metadata.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.runtimeId = runtimeId;
        this.item = item;
        this.position = position;
        this.motion = motion;
        this.metadata = metadata;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        AddItemEntity ret = new AddItemEntity();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "AddItemEntity(entityId: " ~ std.conv.to!string(this.entityId) ~ ", runtimeId: " ~ std.conv.to!string(this.runtimeId) ~ ", item: " ~ std.conv.to!string(this.item) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", motion: " ~ std.conv.to!string(this.motion) ~ ", metadata: " ~ std.conv.to!string(this.metadata) ~ ")";
    }

}

class AddHangingEntity : Pocket113Packet
{

    enum ubyte ID = 16;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["entityId", "runtimeId", "position", "unknown3"];

    @Var long entityId;
    @Var long runtimeId;
    soupply.protocol.pocket113.types.BlockPosition position;
    @Var int unknown3;

    this() pure nothrow @safe @nogc {}

    this(long entityId, long runtimeId=long.init, soupply.protocol.pocket113.types.BlockPosition position=soupply.protocol.pocket113.types.BlockPosition.init, int unknown3=int.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.runtimeId = runtimeId;
        this.position = position;
        this.unknown3 = unknown3;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        AddHangingEntity ret = new AddHangingEntity();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "AddHangingEntity(entityId: " ~ std.conv.to!string(this.entityId) ~ ", runtimeId: " ~ std.conv.to!string(this.runtimeId) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", unknown3: " ~ std.conv.to!string(this.unknown3) ~ ")";
    }

}

class TakeItemEntity : Pocket113Packet
{

    enum ubyte ID = 17;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["collected", "collector"];

    @Var long collected;
    @Var long collector;

    this() pure nothrow @safe @nogc {}

    this(long collected, long collector=long.init) pure nothrow @safe @nogc
    {
        this.collected = collected;
        this.collector = collector;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        TakeItemEntity ret = new TakeItemEntity();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "TakeItemEntity(collected: " ~ std.conv.to!string(this.collected) ~ ", collector: " ~ std.conv.to!string(this.collector) ~ ")";
    }

}

class MoveEntity : Pocket113Packet
{

    enum ubyte ID = 18;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["entityId", "position", "pitch", "headYaw", "yaw", "onGround", "teleported"];

    @Var long entityId;
    Tuple!(float, "xyz") position;
    ubyte pitch;
    ubyte headYaw;
    ubyte yaw;
    bool onGround;
    bool teleported;

    this() pure nothrow @safe @nogc {}

    this(long entityId, Tuple!(float, "xyz") position=Tuple!(float, "xyz").init, ubyte pitch=ubyte.init, ubyte headYaw=ubyte.init, ubyte yaw=ubyte.init, bool onGround=bool.init, bool teleported=bool.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.position = position;
        this.pitch = pitch;
        this.headYaw = headYaw;
        this.yaw = yaw;
        this.onGround = onGround;
        this.teleported = teleported;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        MoveEntity ret = new MoveEntity();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "MoveEntity(entityId: " ~ std.conv.to!string(this.entityId) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", pitch: " ~ std.conv.to!string(this.pitch) ~ ", headYaw: " ~ std.conv.to!string(this.headYaw) ~ ", yaw: " ~ std.conv.to!string(this.yaw) ~ ", onGround: " ~ std.conv.to!string(this.onGround) ~ ", teleported: " ~ std.conv.to!string(this.teleported) ~ ")";
    }

}

class MovePlayer : Pocket113Packet
{

    enum ubyte ID = 19;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    // animation
    enum ubyte FULL = 0;
    enum ubyte NONE = 1;
    enum ubyte TELEPORT = 2;
    enum ubyte PITCH = 3;

    enum string[] __fields = ["entityId", "position", "pitch", "headYaw", "yaw", "animation", "onGround", "unknown7", "unknown8", "unknown9"];

    @Var long entityId;
    Tuple!(float, "xyz") position;
    float pitch;
    float headYaw;
    float yaw;
    ubyte animation;
    bool onGround;
    @Var long unknown7;
    @Condition("animation==3") @LittleEndian int unknown8;
    @Condition("animation==3") @LittleEndian int unknown9;

    this() pure nothrow @safe @nogc {}

    this(long entityId, Tuple!(float, "xyz") position=Tuple!(float, "xyz").init, float pitch=float.init, float headYaw=float.init, float yaw=float.init, ubyte animation=ubyte.init, bool onGround=bool.init, long unknown7=long.init, int unknown8=int.init, int unknown9=int.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.position = position;
        this.pitch = pitch;
        this.headYaw = headYaw;
        this.yaw = yaw;
        this.animation = animation;
        this.onGround = onGround;
        this.unknown7 = unknown7;
        this.unknown8 = unknown8;
        this.unknown9 = unknown9;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        MovePlayer ret = new MovePlayer();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "MovePlayer(entityId: " ~ std.conv.to!string(this.entityId) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", pitch: " ~ std.conv.to!string(this.pitch) ~ ", headYaw: " ~ std.conv.to!string(this.headYaw) ~ ", yaw: " ~ std.conv.to!string(this.yaw) ~ ", animation: " ~ std.conv.to!string(this.animation) ~ ", onGround: " ~ std.conv.to!string(this.onGround) ~ ", unknown7: " ~ std.conv.to!string(this.unknown7) ~ ", unknown8: " ~ std.conv.to!string(this.unknown8) ~ ", unknown9: " ~ std.conv.to!string(this.unknown9) ~ ")";
    }

}

class RiderJump : Pocket113Packet
{

    enum ubyte ID = 20;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["rider"];

    @Var long rider;

    this() pure nothrow @safe @nogc {}

    this(long rider) pure nothrow @safe @nogc
    {
        this.rider = rider;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        RiderJump ret = new RiderJump();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "RiderJump(rider: " ~ std.conv.to!string(this.rider) ~ ")";
    }

}

class RemoveBlock : Pocket113Packet
{

    enum ubyte ID = 21;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["position"];

    soupply.protocol.pocket113.types.BlockPosition position;

    this() pure nothrow @safe @nogc {}

    this(soupply.protocol.pocket113.types.BlockPosition position) pure nothrow @safe @nogc
    {
        this.position = position;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        RemoveBlock ret = new RemoveBlock();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "RemoveBlock(position: " ~ std.conv.to!string(this.position) ~ ")";
    }

}

class UpdateBlock : Pocket113Packet
{

    enum ubyte ID = 22;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    // flags and meta
    enum uint NEIGHBORS = 1;
    enum uint NETWORK = 2;
    enum uint NO_GRAPHIC = 4;
    enum uint PRIORITY = 8;

    enum string[] __fields = ["position", "block", "flagsAndMeta"];

    soupply.protocol.pocket113.types.BlockPosition position;
    @Var uint block;
    @Var uint flagsAndMeta;

    this() pure nothrow @safe @nogc {}

    this(soupply.protocol.pocket113.types.BlockPosition position, uint block=uint.init, uint flagsAndMeta=uint.init) pure nothrow @safe @nogc
    {
        this.position = position;
        this.block = block;
        this.flagsAndMeta = flagsAndMeta;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        UpdateBlock ret = new UpdateBlock();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "UpdateBlock(position: " ~ std.conv.to!string(this.position) ~ ", block: " ~ std.conv.to!string(this.block) ~ ", flagsAndMeta: " ~ std.conv.to!string(this.flagsAndMeta) ~ ")";
    }

}

class AddPainting : Pocket113Packet
{

    enum ubyte ID = 23;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["entityId", "runtimeId", "position", "direction", "title"];

    @Var long entityId;
    @Var long runtimeId;
    soupply.protocol.pocket113.types.BlockPosition position;
    @Var int direction;
    string title;

    this() pure nothrow @safe @nogc {}

    this(long entityId, long runtimeId=long.init, soupply.protocol.pocket113.types.BlockPosition position=soupply.protocol.pocket113.types.BlockPosition.init, int direction=int.init, string title=string.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.runtimeId = runtimeId;
        this.position = position;
        this.direction = direction;
        this.title = title;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        AddPainting ret = new AddPainting();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "AddPainting(entityId: " ~ std.conv.to!string(this.entityId) ~ ", runtimeId: " ~ std.conv.to!string(this.runtimeId) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", direction: " ~ std.conv.to!string(this.direction) ~ ", title: " ~ std.conv.to!string(this.title) ~ ")";
    }

}

class Explode : Pocket113Packet
{

    enum ubyte ID = 24;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["position", "radius", "destroyedBlocks"];

    Tuple!(float, "xyz") position;
    float radius;
    soupply.protocol.pocket113.types.BlockPosition[] destroyedBlocks;

    this() pure nothrow @safe @nogc {}

    this(Tuple!(float, "xyz") position, float radius=float.init, soupply.protocol.pocket113.types.BlockPosition[] destroyedBlocks=(soupply.protocol.pocket113.types.BlockPosition[]).init) pure nothrow @safe @nogc
    {
        this.position = position;
        this.radius = radius;
        this.destroyedBlocks = destroyedBlocks;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        Explode ret = new Explode();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "Explode(position: " ~ std.conv.to!string(this.position) ~ ", radius: " ~ std.conv.to!string(this.radius) ~ ", destroyedBlocks: " ~ std.conv.to!string(this.destroyedBlocks) ~ ")";
    }

}

class LevelSoundEvent : Pocket113Packet
{

    enum ubyte ID = 25;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    // sound
    enum ubyte ITEM_USE_ON = 0;
    enum ubyte HIT = 1;
    enum ubyte STEP = 2;
    enum ubyte JUMP = 3;
    enum ubyte BREAK = 4;
    enum ubyte PLACE = 5;
    enum ubyte HEAVY_STEP = 6;
    enum ubyte GALLOP = 7;
    enum ubyte FALL = 8;
    enum ubyte AMBIENT = 9;
    enum ubyte AMBIENT_BABY = 10;
    enum ubyte AMBIENT_IN_WATER = 11;
    enum ubyte BREATHE = 12;
    enum ubyte DEATH = 13;
    enum ubyte DEATH_IN_WATER = 14;
    enum ubyte DEATH_TO_ZOMBIE = 15;
    enum ubyte HURT = 16;
    enum ubyte HURT_IN_WATER = 17;
    enum ubyte MAD = 18;
    enum ubyte BOOST = 19;
    enum ubyte BOW = 20;
    enum ubyte SQUISH_BIG = 21;
    enum ubyte SQUISH_SMALL = 22;
    enum ubyte FALL_BIG = 23;
    enum ubyte FALL_SMALL = 24;
    enum ubyte SPLASH = 25;
    enum ubyte FIZZ = 26;
    enum ubyte FLAP = 27;
    enum ubyte SWIM = 28;
    enum ubyte DRINK = 29;
    enum ubyte EAT = 30;
    enum ubyte TAKEOFF = 31;
    enum ubyte SHAKE = 32;
    enum ubyte PLOP = 33;
    enum ubyte LAND = 34;
    enum ubyte SADDLE = 35;
    enum ubyte ARMOR = 36;
    enum ubyte ADD_CHEST = 37;
    enum ubyte THROW = 38;
    enum ubyte ATTACK = 39;
    enum ubyte ATTACK_NODAMAGE = 40;
    enum ubyte WARN = 41;
    enum ubyte SHEAR = 42;
    enum ubyte MILK = 43;
    enum ubyte THUNDER = 44;
    enum ubyte EXPLODE = 45;
    enum ubyte FIRE = 46;
    enum ubyte IGNITE = 47;
    enum ubyte FUSE = 48;
    enum ubyte STARE = 49;
    enum ubyte SPAWN = 50;
    enum ubyte SHOOT = 51;
    enum ubyte BREAK_BLOCK = 52;
    enum ubyte REMEDY = 53;
    enum ubyte UNFECT = 54;
    enum ubyte LEVELUP = 55;
    enum ubyte BOW_HIT = 56;
    enum ubyte BULLET_HIT = 57;
    enum ubyte EXTINGUISH_FIRE = 58;
    enum ubyte ITEM_FIZZ = 59;
    enum ubyte CHEST_OPEN = 60;
    enum ubyte CHEST_CLOSED = 61;
    enum ubyte SHULKER_BOX_OPEN = 62;
    enum ubyte SHULKER_BOX_CLOSE = 63;
    enum ubyte POWER_ON = 64;
    enum ubyte POWER_OFF = 65;
    enum ubyte ATTACH = 66;
    enum ubyte DETACH = 67;
    enum ubyte DENY = 68;
    enum ubyte TRIPOD = 69;
    enum ubyte POP = 70;
    enum ubyte DROP_SLOT = 71;
    enum ubyte NOTE = 72;
    enum ubyte THORNS = 73;
    enum ubyte PISTON_IN = 74;
    enum ubyte PISTON_OUT = 75;
    enum ubyte PORTAL = 76;
    enum ubyte WATER = 77;
    enum ubyte LAVA_POP = 78;
    enum ubyte LAVA = 79;
    enum ubyte BURP = 80;
    enum ubyte BUCKET_FILL_WATER = 81;
    enum ubyte BUCKET_FILL_LAVA = 82;
    enum ubyte BUCKET_EMPTY_WATER = 83;
    enum ubyte BUCKET_EMPTY_LAVA = 84;
    enum ubyte GUARDIAN_FLOP = 85;
    enum ubyte ELDERGUARDIAN_CURSE = 86;
    enum ubyte MOB_WARNING = 87;
    enum ubyte MOB_WARNING_BABY = 88;
    enum ubyte TELEPORT = 89;
    enum ubyte SHULKER_OPEN = 90;
    enum ubyte SHULKER_CLOSE = 91;
    enum ubyte HAGGLE = 92;
    enum ubyte HAGGLE_YES = 93;
    enum ubyte HAGGLE_NO = 94;
    enum ubyte HAGGLE_IDLE = 95;
    enum ubyte CHORUS_GROW = 96;
    enum ubyte CHORUS_DEATH = 97;
    enum ubyte GLASS = 98;
    enum ubyte CAST_SPELL = 99;
    enum ubyte PREPARE_ATTACK = 100;
    enum ubyte PREPARE_SUMMON = 101;
    enum ubyte PREPARE_WOLOLO = 102;
    enum ubyte FANG = 103;
    enum ubyte CHARGE = 104;
    enum ubyte TAKE_PICTURE = 105;
    enum ubyte DEFAULT = 106;
    enum ubyte UNDEFINED = 107;

    enum string[] __fields = ["sound", "position", "volume", "pitch", "unknown4"];

    ubyte sound;
    Tuple!(float, "xyz") position;
    @Var uint volume;
    @Var int pitch;
    bool unknown4;

    this() pure nothrow @safe @nogc {}

    this(ubyte sound, Tuple!(float, "xyz") position=Tuple!(float, "xyz").init, uint volume=uint.init, int pitch=int.init, bool unknown4=bool.init) pure nothrow @safe @nogc
    {
        this.sound = sound;
        this.position = position;
        this.volume = volume;
        this.pitch = pitch;
        this.unknown4 = unknown4;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        LevelSoundEvent ret = new LevelSoundEvent();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "LevelSoundEvent(sound: " ~ std.conv.to!string(this.sound) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", volume: " ~ std.conv.to!string(this.volume) ~ ", pitch: " ~ std.conv.to!string(this.pitch) ~ ", unknown4: " ~ std.conv.to!string(this.unknown4) ~ ")";
    }

}

class LevelEvent : Pocket113Packet
{

    enum ubyte ID = 26;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    // event id
    enum int START_RAIN = 3001;
    enum int START_THUNDER = 3002;
    enum int STOP_RAIN = 3003;
    enum int STOP_THUNDER = 3004;
    enum int START_BLOCK_BREAK = 3600;
    enum int STOP_BLOCK_BREAK = 3601;
    enum int SET_DATA = 4000;
    enum int PLAYERS_SLEEPING = 9800;
    enum int PARTICLE_BUBBLE = 16385;
    enum int PARTICLE_CRITICAL = 16386;
    enum int PARTICLE_BLOCK_FORCE_FIELD = 16387;
    enum int PARTICLE_SMOKE = 16388;
    enum int PARTICLE_EXPLODE = 16389;
    enum int PARTICLE_EVAPORATION = 16390;
    enum int PARTICLE_FLAME = 16391;
    enum int PARTICLE_LAVA = 16392;
    enum int PARTICLE_LARGE_SMOKE = 16393;
    enum int PARTICLE_REDSTONE = 16394;
    enum int PARTICLE_RISING_RED_DUST = 16395;
    enum int PARTICLE_ITEM_BREAK = 16396;
    enum int PARTICLE_SNOWBALL_POOF = 16397;
    enum int PARTICLE_HUGE_EXPLODE = 16398;
    enum int PARTICLE_HUGE_EXPLODE_SEED = 16399;
    enum int PARTICLE_MOB_FLAME = 16400;
    enum int PARTICLE_HEART = 16401;
    enum int PARTICLE_TERRAIN = 16402;
    enum int PARTICLE_TOWN_AURA = 16403;
    enum int PARTICLE_PORTAL = 16404;
    enum int PARTICLE_WATER_SPLASH = 16405;
    enum int PARTICLE_WATER_WAKE = 16406;
    enum int PARTICLE_DRIP_WATER = 16407;
    enum int PARTICLE_DRIP_LAVA = 16408;
    enum int PARTICLE_FALLING_DUST = 16409;
    enum int PARTICLE_MOB_SPELL = 16410;
    enum int PARTICLE_MOB_SPELL_AMBIENT = 16411;
    enum int PARTICLE_MOB_SPELL_INSTANTANEOUS = 16412;
    enum int PARTICLE_INK = 16413;
    enum int PARTICLE_SLIME = 16414;
    enum int PARTICLE_RAIN_SPLASH = 16415;
    enum int PARTICLE_VILLAGER_ANGRY = 16416;
    enum int PARTICLE_VILLAGER_HAPPY = 16417;
    enum int PARTICLE_ENCHANTMENT_TABLE = 16418;
    enum int PARTICLE_TRACKING_EMITTER = 16419;
    enum int PARTICLE_NOTE = 16420;
    enum int PARTICLE_WITCH_SPELL = 16421;
    enum int PARTICLE_CARROT = 16422;
    enum int PARTICLE_END_ROD = 16424;
    enum int PARTICLE_DRAGON_BREATH = 16425;
    enum int PARTICLE_SHOOT = 2000;
    enum int PARTICLE_DESTROY = 2001;
    enum int PARTICLE_SPLASH = 2002;
    enum int PARTICLE_EYE_DESPAWN = 2003;
    enum int PARTICLE_SPAWN = 2004;

    enum string[] __fields = ["eventId", "position", "data"];

    @Var int eventId;
    Tuple!(float, "xyz") position;
    @Var int data;

    this() pure nothrow @safe @nogc {}

    this(int eventId, Tuple!(float, "xyz") position=Tuple!(float, "xyz").init, int data=int.init) pure nothrow @safe @nogc
    {
        this.eventId = eventId;
        this.position = position;
        this.data = data;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        LevelEvent ret = new LevelEvent();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "LevelEvent(eventId: " ~ std.conv.to!string(this.eventId) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", data: " ~ std.conv.to!string(this.data) ~ ")";
    }

}

class BlockEvent : Pocket113Packet
{

    enum ubyte ID = 27;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["position", "data"];

    soupply.protocol.pocket113.types.BlockPosition position;
    @Var int[2] data;

    this() pure nothrow @safe @nogc {}

    this(soupply.protocol.pocket113.types.BlockPosition position, int[2] data=(int[2]).init) pure nothrow @safe @nogc
    {
        this.position = position;
        this.data = data;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        BlockEvent ret = new BlockEvent();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "BlockEvent(position: " ~ std.conv.to!string(this.position) ~ ", data: " ~ std.conv.to!string(this.data) ~ ")";
    }

}

class EntityEvent : Pocket113Packet
{

    enum ubyte ID = 28;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    // event id
    enum ubyte HURT_ANIMATION = 2;
    enum ubyte DEATH_ANIMATION = 3;
    enum ubyte TAME_FAIL = 6;
    enum ubyte TAME_SUCCESS = 7;
    enum ubyte SHAKE_WET = 8;
    enum ubyte USE_ITEM = 9;
    enum ubyte EAT_GRASS_ANIMATION = 10;
    enum ubyte FISH_HOOK_BUBBLES = 11;
    enum ubyte FISH_HOOK_POSITION = 12;
    enum ubyte FISH_HOOK_HOOK = 13;
    enum ubyte FISH_HOOK_TEASE = 14;
    enum ubyte SQUID_INK_CLOUD = 15;
    enum ubyte AMBIENT_SOUND = 16;
    enum ubyte RESPAWN = 17;
    enum ubyte UNLEASH = 63;

    enum string[] __fields = ["entityId", "eventId", "unknown2"];

    @Var long entityId;
    ubyte eventId;
    @Var int unknown2;

    this() pure nothrow @safe @nogc {}

    this(long entityId, ubyte eventId=ubyte.init, int unknown2=int.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.eventId = eventId;
        this.unknown2 = unknown2;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        EntityEvent ret = new EntityEvent();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "EntityEvent(entityId: " ~ std.conv.to!string(this.entityId) ~ ", eventId: " ~ std.conv.to!string(this.eventId) ~ ", unknown2: " ~ std.conv.to!string(this.unknown2) ~ ")";
    }

}

class MobEffect : Pocket113Packet
{

    enum ubyte ID = 29;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    // event id
    enum ubyte ADD = 1;
    enum ubyte MODIFY = 2;
    enum ubyte REMOVE = 3;

    enum string[] __fields = ["entityId", "eventId", "effect", "amplifier", "particles", "duration"];

    @Var long entityId;
    ubyte eventId;
    @Var int effect;
    @Var int amplifier;
    bool particles;
    @Var int duration;

    this() pure nothrow @safe @nogc {}

    this(long entityId, ubyte eventId=ubyte.init, int effect=int.init, int amplifier=int.init, bool particles=bool.init, int duration=int.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.eventId = eventId;
        this.effect = effect;
        this.amplifier = amplifier;
        this.particles = particles;
        this.duration = duration;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        MobEffect ret = new MobEffect();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "MobEffect(entityId: " ~ std.conv.to!string(this.entityId) ~ ", eventId: " ~ std.conv.to!string(this.eventId) ~ ", effect: " ~ std.conv.to!string(this.effect) ~ ", amplifier: " ~ std.conv.to!string(this.amplifier) ~ ", particles: " ~ std.conv.to!string(this.particles) ~ ", duration: " ~ std.conv.to!string(this.duration) ~ ")";
    }

}

class UpdateAttributes : Pocket113Packet
{

    enum ubyte ID = 30;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["entityId", "attributes"];

    @Var long entityId;
    soupply.protocol.pocket113.types.Attribute[] attributes;

    this() pure nothrow @safe @nogc {}

    this(long entityId, soupply.protocol.pocket113.types.Attribute[] attributes=(soupply.protocol.pocket113.types.Attribute[]).init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.attributes = attributes;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        UpdateAttributes ret = new UpdateAttributes();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "UpdateAttributes(entityId: " ~ std.conv.to!string(this.entityId) ~ ", attributes: " ~ std.conv.to!string(this.attributes) ~ ")";
    }

}

class MobEquipment : Pocket113Packet
{

    enum ubyte ID = 31;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["entityId", "item", "inventorySlot", "hotbarSlot", "unknown4"];

    @Var long entityId;
    soupply.protocol.pocket113.types.Slot item;
    ubyte inventorySlot;
    ubyte hotbarSlot;
    ubyte unknown4;

    this() pure nothrow @safe @nogc {}

    this(long entityId, soupply.protocol.pocket113.types.Slot item=soupply.protocol.pocket113.types.Slot.init, ubyte inventorySlot=ubyte.init, ubyte hotbarSlot=ubyte.init, ubyte unknown4=ubyte.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.item = item;
        this.inventorySlot = inventorySlot;
        this.hotbarSlot = hotbarSlot;
        this.unknown4 = unknown4;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        MobEquipment ret = new MobEquipment();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "MobEquipment(entityId: " ~ std.conv.to!string(this.entityId) ~ ", item: " ~ std.conv.to!string(this.item) ~ ", inventorySlot: " ~ std.conv.to!string(this.inventorySlot) ~ ", hotbarSlot: " ~ std.conv.to!string(this.hotbarSlot) ~ ", unknown4: " ~ std.conv.to!string(this.unknown4) ~ ")";
    }

}

class MobArmorEquipment : Pocket113Packet
{

    enum ubyte ID = 32;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["entityId", "armor"];

    @Var long entityId;
    soupply.protocol.pocket113.types.Slot[4] armor;

    this() pure nothrow @safe @nogc {}

    this(long entityId, soupply.protocol.pocket113.types.Slot[4] armor=(soupply.protocol.pocket113.types.Slot[4]).init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.armor = armor;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        MobArmorEquipment ret = new MobArmorEquipment();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "MobArmorEquipment(entityId: " ~ std.conv.to!string(this.entityId) ~ ", armor: " ~ std.conv.to!string(this.armor) ~ ")";
    }

}

class Interact : Pocket113Packet
{

    enum ubyte ID = 33;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    // action
    enum ubyte INTERACT = 1;
    enum ubyte ATTACK = 2;
    enum ubyte LEAVE_VEHICLE = 3;
    enum ubyte HOVER = 4;

    enum string[] __fields = ["action", "target"];

    ubyte action;
    @Var long target;

    this() pure nothrow @safe @nogc {}

    this(ubyte action, long target=long.init) pure nothrow @safe @nogc
    {
        this.action = action;
        this.target = target;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        Interact ret = new Interact();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "Interact(action: " ~ std.conv.to!string(this.action) ~ ", target: " ~ std.conv.to!string(this.target) ~ ")";
    }

}

class BlockPickRequest : Pocket113Packet
{

    enum ubyte ID = 34;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["position", "slot"];

    @Var Tuple!(int, "xyz") position;
    ubyte slot;

    this() pure nothrow @safe @nogc {}

    this(Tuple!(int, "xyz") position, ubyte slot=ubyte.init) pure nothrow @safe @nogc
    {
        this.position = position;
        this.slot = slot;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        BlockPickRequest ret = new BlockPickRequest();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "BlockPickRequest(position: " ~ std.conv.to!string(this.position) ~ ", slot: " ~ std.conv.to!string(this.slot) ~ ")";
    }

}

class UseItem : Pocket113Packet
{

    enum ubyte ID = 35;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["blockPosition", "hotbarSlot", "face", "facePosition", "position", "slot", "item"];

    soupply.protocol.pocket113.types.BlockPosition blockPosition;
    @Var uint hotbarSlot;
    @Var int face;
    Tuple!(float, "xyz") facePosition;
    Tuple!(float, "xyz") position;
    @Var int slot;
    soupply.protocol.pocket113.types.Slot item;

    this() pure nothrow @safe @nogc {}

    this(soupply.protocol.pocket113.types.BlockPosition blockPosition, uint hotbarSlot=uint.init, int face=int.init, Tuple!(float, "xyz") facePosition=Tuple!(float, "xyz").init, Tuple!(float, "xyz") position=Tuple!(float, "xyz").init, int slot=int.init, soupply.protocol.pocket113.types.Slot item=soupply.protocol.pocket113.types.Slot.init) pure nothrow @safe @nogc
    {
        this.blockPosition = blockPosition;
        this.hotbarSlot = hotbarSlot;
        this.face = face;
        this.facePosition = facePosition;
        this.position = position;
        this.slot = slot;
        this.item = item;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        UseItem ret = new UseItem();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "UseItem(blockPosition: " ~ std.conv.to!string(this.blockPosition) ~ ", hotbarSlot: " ~ std.conv.to!string(this.hotbarSlot) ~ ", face: " ~ std.conv.to!string(this.face) ~ ", facePosition: " ~ std.conv.to!string(this.facePosition) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", slot: " ~ std.conv.to!string(this.slot) ~ ", item: " ~ std.conv.to!string(this.item) ~ ")";
    }

}

class PlayerAction : Pocket113Packet
{

    enum ubyte ID = 36;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    // action
    enum int START_BREAK = 0;
    enum int ABORT_BREAK = 1;
    enum int STOP_BREAK = 2;
    enum int RELEASE_ITEM = 5;
    enum int STOP_SLEEPING = 6;
    enum int RESPAWN = 7;
    enum int JUMP = 8;
    enum int START_SPRINT = 9;
    enum int STOP_SPRINT = 10;
    enum int START_SNEAK = 11;
    enum int STOP_SNEAK = 12;
    enum int START_GLIDING = 15;
    enum int STOP_GLIDING = 16;
    enum int CONTINUE_BREAK = 18;

    enum string[] __fields = ["entityId", "action", "position", "face"];

    @Var long entityId;
    @Var int action;
    soupply.protocol.pocket113.types.BlockPosition position;
    @Var int face;

    this() pure nothrow @safe @nogc {}

    this(long entityId, int action=int.init, soupply.protocol.pocket113.types.BlockPosition position=soupply.protocol.pocket113.types.BlockPosition.init, int face=int.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.action = action;
        this.position = position;
        this.face = face;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        PlayerAction ret = new PlayerAction();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "PlayerAction(entityId: " ~ std.conv.to!string(this.entityId) ~ ", action: " ~ std.conv.to!string(this.action) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", face: " ~ std.conv.to!string(this.face) ~ ")";
    }

}

class EntityFall : Pocket113Packet
{

    enum ubyte ID = 37;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["entityId", "distance", "unknown2"];

    @Var long entityId;
    float distance;
    bool unknown2;

    this() pure nothrow @safe @nogc {}

    this(long entityId, float distance=float.init, bool unknown2=bool.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.distance = distance;
        this.unknown2 = unknown2;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        EntityFall ret = new EntityFall();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "EntityFall(entityId: " ~ std.conv.to!string(this.entityId) ~ ", distance: " ~ std.conv.to!string(this.distance) ~ ", unknown2: " ~ std.conv.to!string(this.unknown2) ~ ")";
    }

}

class HurtArmor : Pocket113Packet
{

    enum ubyte ID = 38;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["unknown0"];

    @Var int unknown0;

    this() pure nothrow @safe @nogc {}

    this(int unknown0) pure nothrow @safe @nogc
    {
        this.unknown0 = unknown0;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        HurtArmor ret = new HurtArmor();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "HurtArmor(unknown0: " ~ std.conv.to!string(this.unknown0) ~ ")";
    }

}

class SetEntityData : Pocket113Packet
{

    enum ubyte ID = 39;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["entityId", "metadata"];

    @Var long entityId;
    Metadata metadata;

    this() pure nothrow @safe @nogc {}

    this(long entityId, Metadata metadata=Metadata.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.metadata = metadata;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        SetEntityData ret = new SetEntityData();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "SetEntityData(entityId: " ~ std.conv.to!string(this.entityId) ~ ", metadata: " ~ std.conv.to!string(this.metadata) ~ ")";
    }

}

class SetEntityMotion : Pocket113Packet
{

    enum ubyte ID = 40;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["entityId", "motion"];

    @Var long entityId;
    Tuple!(float, "xyz") motion;

    this() pure nothrow @safe @nogc {}

    this(long entityId, Tuple!(float, "xyz") motion=Tuple!(float, "xyz").init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.motion = motion;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        SetEntityMotion ret = new SetEntityMotion();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "SetEntityMotion(entityId: " ~ std.conv.to!string(this.entityId) ~ ", motion: " ~ std.conv.to!string(this.motion) ~ ")";
    }

}

class SetEntityLink : Pocket113Packet
{

    enum ubyte ID = 41;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    // action
    enum ubyte REMOVE = 0;
    enum ubyte ADD = 1;

    enum string[] __fields = ["vehicle", "passenger", "action"];

    @Var long vehicle;
    @Var long passenger;
    ubyte action;

    this() pure nothrow @safe @nogc {}

    this(long vehicle, long passenger=long.init, ubyte action=ubyte.init) pure nothrow @safe @nogc
    {
        this.vehicle = vehicle;
        this.passenger = passenger;
        this.action = action;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        SetEntityLink ret = new SetEntityLink();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "SetEntityLink(vehicle: " ~ std.conv.to!string(this.vehicle) ~ ", passenger: " ~ std.conv.to!string(this.passenger) ~ ", action: " ~ std.conv.to!string(this.action) ~ ")";
    }

}

class SetHealth : Pocket113Packet
{

    enum ubyte ID = 42;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["health"];

    @Var int health;

    this() pure nothrow @safe @nogc {}

    this(int health) pure nothrow @safe @nogc
    {
        this.health = health;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        SetHealth ret = new SetHealth();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "SetHealth(health: " ~ std.conv.to!string(this.health) ~ ")";
    }

}

class SetSpawnPosition : Pocket113Packet
{

    enum ubyte ID = 43;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    // type
    enum int PLAYER_SPAWN = 0;
    enum int WORLD_SPAWN = 1;

    enum string[] __fields = ["type", "position", "forced"];

    @Var int type;
    soupply.protocol.pocket113.types.BlockPosition position;
    bool forced;

    this() pure nothrow @safe @nogc {}

    this(int type, soupply.protocol.pocket113.types.BlockPosition position=soupply.protocol.pocket113.types.BlockPosition.init, bool forced=bool.init) pure nothrow @safe @nogc
    {
        this.type = type;
        this.position = position;
        this.forced = forced;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        SetSpawnPosition ret = new SetSpawnPosition();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "SetSpawnPosition(type: " ~ std.conv.to!string(this.type) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", forced: " ~ std.conv.to!string(this.forced) ~ ")";
    }

}

class Animate : Pocket113Packet
{

    enum ubyte ID = 44;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    // action
    enum int BREAKING = 1;
    enum int WAKE_UP = 3;

    enum string[] __fields = ["action", "entityId", "unknown2"];

    @Var int action;
    @Var long entityId;
    @Condition("action>128") float unknown2;

    this() pure nothrow @safe @nogc {}

    this(int action, long entityId=long.init, float unknown2=float.init) pure nothrow @safe @nogc
    {
        this.action = action;
        this.entityId = entityId;
        this.unknown2 = unknown2;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        Animate ret = new Animate();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "Animate(action: " ~ std.conv.to!string(this.action) ~ ", entityId: " ~ std.conv.to!string(this.entityId) ~ ", unknown2: " ~ std.conv.to!string(this.unknown2) ~ ")";
    }

}

class Respawn : Pocket113Packet
{

    enum ubyte ID = 45;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["position"];

    Tuple!(float, "xyz") position;

    this() pure nothrow @safe @nogc {}

    this(Tuple!(float, "xyz") position) pure nothrow @safe @nogc
    {
        this.position = position;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        Respawn ret = new Respawn();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "Respawn(position: " ~ std.conv.to!string(this.position) ~ ")";
    }

}

class DropItem : Pocket113Packet
{

    enum ubyte ID = 46;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    // action
    enum ubyte DROP = 0;

    enum string[] __fields = ["action", "item"];

    ubyte action;
    soupply.protocol.pocket113.types.Slot item;

    this() pure nothrow @safe @nogc {}

    this(ubyte action, soupply.protocol.pocket113.types.Slot item=soupply.protocol.pocket113.types.Slot.init) pure nothrow @safe @nogc
    {
        this.action = action;
        this.item = item;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        DropItem ret = new DropItem();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "DropItem(action: " ~ std.conv.to!string(this.action) ~ ", item: " ~ std.conv.to!string(this.item) ~ ")";
    }

}

class InventoryAction : Pocket113Packet
{

    enum ubyte ID = 47;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["action", "item", "unknown2", "unknown3"];

    @Var int action;
    soupply.protocol.pocket113.types.Slot item;
    @Var int unknown2;
    @Var int unknown3;

    this() pure nothrow @safe @nogc {}

    this(int action, soupply.protocol.pocket113.types.Slot item=soupply.protocol.pocket113.types.Slot.init, int unknown2=int.init, int unknown3=int.init) pure nothrow @safe @nogc
    {
        this.action = action;
        this.item = item;
        this.unknown2 = unknown2;
        this.unknown3 = unknown3;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        InventoryAction ret = new InventoryAction();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "InventoryAction(action: " ~ std.conv.to!string(this.action) ~ ", item: " ~ std.conv.to!string(this.item) ~ ", unknown2: " ~ std.conv.to!string(this.unknown2) ~ ", unknown3: " ~ std.conv.to!string(this.unknown3) ~ ")";
    }

}

class ContainerOpen : Pocket113Packet
{

    enum ubyte ID = 48;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["window", "type", "position", "entityId"];

    ubyte window;
    ubyte type;
    soupply.protocol.pocket113.types.BlockPosition position;
    @Var long entityId;

    this() pure nothrow @safe @nogc {}

    this(ubyte window, ubyte type=ubyte.init, soupply.protocol.pocket113.types.BlockPosition position=soupply.protocol.pocket113.types.BlockPosition.init, long entityId=long.init) pure nothrow @safe @nogc
    {
        this.window = window;
        this.type = type;
        this.position = position;
        this.entityId = entityId;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ContainerOpen ret = new ContainerOpen();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ContainerOpen(window: " ~ std.conv.to!string(this.window) ~ ", type: " ~ std.conv.to!string(this.type) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", entityId: " ~ std.conv.to!string(this.entityId) ~ ")";
    }

}

class ContainerClose : Pocket113Packet
{

    enum ubyte ID = 49;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["window"];

    ubyte window;

    this() pure nothrow @safe @nogc {}

    this(ubyte window) pure nothrow @safe @nogc
    {
        this.window = window;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ContainerClose ret = new ContainerClose();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ContainerClose(window: " ~ std.conv.to!string(this.window) ~ ")";
    }

}

class ContainerSetSlot : Pocket113Packet
{

    enum ubyte ID = 50;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["window", "slot", "hotbarSlot", "item", "unknown4"];

    ubyte window;
    @Var int slot;
    @Var int hotbarSlot;
    soupply.protocol.pocket113.types.Slot item;
    ubyte unknown4;

    this() pure nothrow @safe @nogc {}

    this(ubyte window, int slot=int.init, int hotbarSlot=int.init, soupply.protocol.pocket113.types.Slot item=soupply.protocol.pocket113.types.Slot.init, ubyte unknown4=ubyte.init) pure nothrow @safe @nogc
    {
        this.window = window;
        this.slot = slot;
        this.hotbarSlot = hotbarSlot;
        this.item = item;
        this.unknown4 = unknown4;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ContainerSetSlot ret = new ContainerSetSlot();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ContainerSetSlot(window: " ~ std.conv.to!string(this.window) ~ ", slot: " ~ std.conv.to!string(this.slot) ~ ", hotbarSlot: " ~ std.conv.to!string(this.hotbarSlot) ~ ", item: " ~ std.conv.to!string(this.item) ~ ", unknown4: " ~ std.conv.to!string(this.unknown4) ~ ")";
    }

}

class ContainerSetData : Pocket113Packet
{

    enum ubyte ID = 51;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["window", "property", "value"];

    ubyte window;
    @Var int property;
    @Var int value;

    this() pure nothrow @safe @nogc {}

    this(ubyte window, int property=int.init, int value=int.init) pure nothrow @safe @nogc
    {
        this.window = window;
        this.property = property;
        this.value = value;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ContainerSetData ret = new ContainerSetData();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ContainerSetData(window: " ~ std.conv.to!string(this.window) ~ ", property: " ~ std.conv.to!string(this.property) ~ ", value: " ~ std.conv.to!string(this.value) ~ ")";
    }

}

class ContainerSetContent : Pocket113Packet
{

    enum ubyte ID = 52;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["window", "entityId", "slots", "hotbar"];

    @Var uint window;
    @Var long entityId;
    soupply.protocol.pocket113.types.Slot[] slots;
    @Var int[] hotbar;

    this() pure nothrow @safe @nogc {}

    this(uint window, long entityId=long.init, soupply.protocol.pocket113.types.Slot[] slots=(soupply.protocol.pocket113.types.Slot[]).init, int[] hotbar=(int[]).init) pure nothrow @safe @nogc
    {
        this.window = window;
        this.entityId = entityId;
        this.slots = slots;
        this.hotbar = hotbar;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ContainerSetContent ret = new ContainerSetContent();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ContainerSetContent(window: " ~ std.conv.to!string(this.window) ~ ", entityId: " ~ std.conv.to!string(this.entityId) ~ ", slots: " ~ std.conv.to!string(this.slots) ~ ", hotbar: " ~ std.conv.to!string(this.hotbar) ~ ")";
    }

}

class CraftingData : Pocket113Packet
{

    enum ubyte ID = 53;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["recipes"];

    soupply.protocol.pocket113.types.Recipe[] recipes;

    this() pure nothrow @safe @nogc {}

    this(soupply.protocol.pocket113.types.Recipe[] recipes) pure nothrow @safe @nogc
    {
        this.recipes = recipes;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        CraftingData ret = new CraftingData();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "CraftingData(recipes: " ~ std.conv.to!string(this.recipes) ~ ")";
    }

}

class CraftingEvent : Pocket113Packet
{

    enum ubyte ID = 54;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["window", "type", "uuid", "input", "output"];

    ubyte window;
    @Var int type;
    soupply.protocol.pocket113.types.McpeUuid uuid;
    soupply.protocol.pocket113.types.Slot[] input;
    soupply.protocol.pocket113.types.Slot[] output;

    this() pure nothrow @safe @nogc {}

    this(ubyte window, int type=int.init, soupply.protocol.pocket113.types.McpeUuid uuid=soupply.protocol.pocket113.types.McpeUuid.init, soupply.protocol.pocket113.types.Slot[] input=(soupply.protocol.pocket113.types.Slot[]).init, soupply.protocol.pocket113.types.Slot[] output=(soupply.protocol.pocket113.types.Slot[]).init) pure nothrow @safe @nogc
    {
        this.window = window;
        this.type = type;
        this.uuid = uuid;
        this.input = input;
        this.output = output;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        CraftingEvent ret = new CraftingEvent();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "CraftingEvent(window: " ~ std.conv.to!string(this.window) ~ ", type: " ~ std.conv.to!string(this.type) ~ ", uuid: " ~ std.conv.to!string(this.uuid) ~ ", input: " ~ std.conv.to!string(this.input) ~ ", output: " ~ std.conv.to!string(this.output) ~ ")";
    }

}

class AdventureSettings : Pocket113Packet
{

    enum ubyte ID = 55;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    // flags
    enum uint IMMUTABLE_WORLD = 1;
    enum uint PVP_DISABLED = 2;
    enum uint PVM_DISABLED = 4;
    enum uint MVP_DISBALED = 8;
    enum uint EVP_DISABLED = 16;
    enum uint AUTO_JUMP = 32;
    enum uint ALLOW_FLIGHT = 64;
    enum uint NO_CLIP = 128;
    enum uint FLYING = 512;
    enum uint MUTED = 1024;

    // permissions
    enum uint USER = 0;
    enum uint OPERATOR = 1;
    enum uint HOST = 2;
    enum uint AUTOMATION = 3;
    enum uint ADMIN = 4;

    enum string[] __fields = ["flags", "permissions"];

    @Var uint flags;
    @Var uint permissions;

    this() pure nothrow @safe @nogc {}

    this(uint flags, uint permissions=uint.init) pure nothrow @safe @nogc
    {
        this.flags = flags;
        this.permissions = permissions;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        AdventureSettings ret = new AdventureSettings();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "AdventureSettings(flags: " ~ std.conv.to!string(this.flags) ~ ", permissions: " ~ std.conv.to!string(this.permissions) ~ ")";
    }

}

class BlockEntityData : Pocket113Packet
{

    enum ubyte ID = 56;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["position", "nbt"];

    soupply.protocol.pocket113.types.BlockPosition position;
    @Bytes ubyte[] nbt;

    this() pure nothrow @safe @nogc {}

    this(soupply.protocol.pocket113.types.BlockPosition position, ubyte[] nbt=(ubyte[]).init) pure nothrow @safe @nogc
    {
        this.position = position;
        this.nbt = nbt;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        BlockEntityData ret = new BlockEntityData();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "BlockEntityData(position: " ~ std.conv.to!string(this.position) ~ ", nbt: " ~ std.conv.to!string(this.nbt) ~ ")";
    }

}

class PlayerInput : Pocket113Packet
{

    enum ubyte ID = 57;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["sideways", "forward", "unknown2", "unknown3"];

    float sideways;
    float forward;
    bool unknown2;
    bool unknown3;

    this() pure nothrow @safe @nogc {}

    this(float sideways, float forward=float.init, bool unknown2=bool.init, bool unknown3=bool.init) pure nothrow @safe @nogc
    {
        this.sideways = sideways;
        this.forward = forward;
        this.unknown2 = unknown2;
        this.unknown3 = unknown3;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        PlayerInput ret = new PlayerInput();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "PlayerInput(sideways: " ~ std.conv.to!string(this.sideways) ~ ", forward: " ~ std.conv.to!string(this.forward) ~ ", unknown2: " ~ std.conv.to!string(this.unknown2) ~ ", unknown3: " ~ std.conv.to!string(this.unknown3) ~ ")";
    }

}

class FullChunkData : Pocket113Packet
{

    enum ubyte ID = 58;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["position", "data"];

    @Var Tuple!(int, "xz") position;
    soupply.protocol.pocket113.types.ChunkData data;

    this() pure nothrow @safe @nogc {}

    this(Tuple!(int, "xz") position, soupply.protocol.pocket113.types.ChunkData data=soupply.protocol.pocket113.types.ChunkData.init) pure nothrow @safe @nogc
    {
        this.position = position;
        this.data = data;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        FullChunkData ret = new FullChunkData();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "FullChunkData(position: " ~ std.conv.to!string(this.position) ~ ", data: " ~ std.conv.to!string(this.data) ~ ")";
    }

}

class SetCommandsEnabled : Pocket113Packet
{

    enum ubyte ID = 59;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["enabled"];

    bool enabled;

    this() pure nothrow @safe @nogc {}

    this(bool enabled) pure nothrow @safe @nogc
    {
        this.enabled = enabled;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        SetCommandsEnabled ret = new SetCommandsEnabled();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "SetCommandsEnabled(enabled: " ~ std.conv.to!string(this.enabled) ~ ")";
    }

}

class SetDifficulty : Pocket113Packet
{

    enum ubyte ID = 60;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    // difficulty
    enum uint PEACEFUL = 0;
    enum uint EASY = 1;
    enum uint NORMAL = 2;
    enum uint HARD = 3;

    enum string[] __fields = ["difficulty"];

    @Var uint difficulty;

    this() pure nothrow @safe @nogc {}

    this(uint difficulty) pure nothrow @safe @nogc
    {
        this.difficulty = difficulty;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        SetDifficulty ret = new SetDifficulty();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "SetDifficulty(difficulty: " ~ std.conv.to!string(this.difficulty) ~ ")";
    }

}

class ChangeDimension : Pocket113Packet
{

    enum ubyte ID = 61;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    // dimension
    enum int OVERWORLD = 0;
    enum int NETHER = 1;
    enum int END = 2;

    enum string[] __fields = ["dimension", "position", "unknown2"];

    @Var int dimension;
    Tuple!(float, "xyz") position;
    bool unknown2;

    this() pure nothrow @safe @nogc {}

    this(int dimension, Tuple!(float, "xyz") position=Tuple!(float, "xyz").init, bool unknown2=bool.init) pure nothrow @safe @nogc
    {
        this.dimension = dimension;
        this.position = position;
        this.unknown2 = unknown2;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ChangeDimension ret = new ChangeDimension();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ChangeDimension(dimension: " ~ std.conv.to!string(this.dimension) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", unknown2: " ~ std.conv.to!string(this.unknown2) ~ ")";
    }

}

class SetPlayerGameType : Pocket113Packet
{

    enum ubyte ID = 62;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    // gamemode
    enum int SURVIVAL = 0;
    enum int CREATIVE = 1;
    enum int ADVENTURE = 2;

    enum string[] __fields = ["gamemode"];

    @Var int gamemode;

    this() pure nothrow @safe @nogc {}

    this(int gamemode) pure nothrow @safe @nogc
    {
        this.gamemode = gamemode;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        SetPlayerGameType ret = new SetPlayerGameType();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "SetPlayerGameType(gamemode: " ~ std.conv.to!string(this.gamemode) ~ ")";
    }

}

class PlayerList : Pocket113Packet
{

    enum ubyte ID = 63;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["action"];

    ubyte action;

    this() pure nothrow @safe @nogc {}

    this(ubyte action) pure nothrow @safe @nogc
    {
        this.action = action;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        PlayerList ret = new PlayerList();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "PlayerList(action: " ~ std.conv.to!string(this.action) ~ ")";
    }

    enum string variantField = "action";

    alias Variants = TypeTuple!(Add, Remove);

    class Add : Pocket113Packet
    {

        enum typeof(action) ACTION = 0;

        enum string[] __fields = ["players"];

        soupply.protocol.pocket113.types.PlayerList[] players;

        this() pure nothrow @safe @nogc {}

        this(soupply.protocol.pocket113.types.PlayerList[] players) pure nothrow @safe @nogc
        {
            this.players = players;
        }

        mixin MakeNested;

        public override string toString()
        {
            return "PlayerList.Add(players: " ~ std.conv.to!string(this.players) ~ ")";
        }

    }

    class Remove : Pocket113Packet
    {

        enum typeof(action) ACTION = 1;

        enum string[] __fields = ["players"];

        soupply.protocol.pocket113.types.McpeUuid[] players;

        this() pure nothrow @safe @nogc {}

        this(soupply.protocol.pocket113.types.McpeUuid[] players) pure nothrow @safe @nogc
        {
            this.players = players;
        }

        mixin MakeNested;

        public override string toString()
        {
            return "PlayerList.Remove(players: " ~ std.conv.to!string(this.players) ~ ")";
        }

    }

}

class SimpleEvent : Pocket113Packet
{

    enum ubyte ID = 64;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    enum string[] __fields = [];

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        SimpleEvent ret = new SimpleEvent();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "SimpleEvent()";
    }

}

class TelemetryEvent : Pocket113Packet
{

    enum ubyte ID = 65;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["entityId", "eventId"];

    @Var long entityId;
    @Var int eventId;

    this() pure nothrow @safe @nogc {}

    this(long entityId, int eventId=int.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.eventId = eventId;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        TelemetryEvent ret = new TelemetryEvent();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "TelemetryEvent(entityId: " ~ std.conv.to!string(this.entityId) ~ ", eventId: " ~ std.conv.to!string(this.eventId) ~ ")";
    }

}

class SpawnExperienceOrb : Pocket113Packet
{

    enum ubyte ID = 66;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["position", "count"];

    Tuple!(float, "xyz") position;
    @Var int count;

    this() pure nothrow @safe @nogc {}

    this(Tuple!(float, "xyz") position, int count=int.init) pure nothrow @safe @nogc
    {
        this.position = position;
        this.count = count;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        SpawnExperienceOrb ret = new SpawnExperienceOrb();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "SpawnExperienceOrb(position: " ~ std.conv.to!string(this.position) ~ ", count: " ~ std.conv.to!string(this.count) ~ ")";
    }

}

class ClientboundMapItemData : Pocket113Packet
{

    enum ubyte ID = 67;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    // update
    enum uint TEXTURE = 2;
    enum uint DECORATIONS = 4;
    enum uint ENTITIES = 8;

    enum string[] __fields = ["mapId", "update", "scale", "size", "offset", "data", "decorations"];

    @Var long mapId;
    @Var uint update;
    @Condition("update==2||update==4") ubyte scale;
    @Condition("update==2") @Var Tuple!(int, "xz") size;
    @Condition("update==2") @Var Tuple!(int, "xz") offset;
    @Condition("update==2") @Bytes ubyte[] data;
    @Condition("update==4") soupply.protocol.pocket113.types.Decoration[] decorations;

    this() pure nothrow @safe @nogc {}

    this(long mapId, uint update=uint.init, ubyte scale=ubyte.init, Tuple!(int, "xz") size=Tuple!(int, "xz").init, Tuple!(int, "xz") offset=Tuple!(int, "xz").init, ubyte[] data=(ubyte[]).init, soupply.protocol.pocket113.types.Decoration[] decorations=(soupply.protocol.pocket113.types.Decoration[]).init) pure nothrow @safe @nogc
    {
        this.mapId = mapId;
        this.update = update;
        this.scale = scale;
        this.size = size;
        this.offset = offset;
        this.data = data;
        this.decorations = decorations;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ClientboundMapItemData ret = new ClientboundMapItemData();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ClientboundMapItemData(mapId: " ~ std.conv.to!string(this.mapId) ~ ", update: " ~ std.conv.to!string(this.update) ~ ", scale: " ~ std.conv.to!string(this.scale) ~ ", size: " ~ std.conv.to!string(this.size) ~ ", offset: " ~ std.conv.to!string(this.offset) ~ ", data: " ~ std.conv.to!string(this.data) ~ ", decorations: " ~ std.conv.to!string(this.decorations) ~ ")";
    }

}

class MapInfoRequest : Pocket113Packet
{

    enum ubyte ID = 68;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["mapId"];

    @Var long mapId;

    this() pure nothrow @safe @nogc {}

    this(long mapId) pure nothrow @safe @nogc
    {
        this.mapId = mapId;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        MapInfoRequest ret = new MapInfoRequest();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "MapInfoRequest(mapId: " ~ std.conv.to!string(this.mapId) ~ ")";
    }

}

class RequestChunkRadius : Pocket113Packet
{

    enum ubyte ID = 69;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["radius"];

    @Var int radius;

    this() pure nothrow @safe @nogc {}

    this(int radius) pure nothrow @safe @nogc
    {
        this.radius = radius;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        RequestChunkRadius ret = new RequestChunkRadius();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "RequestChunkRadius(radius: " ~ std.conv.to!string(this.radius) ~ ")";
    }

}

class ChunkRadiusUpdated : Pocket113Packet
{

    enum ubyte ID = 70;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["radius"];

    @Var int radius;

    this() pure nothrow @safe @nogc {}

    this(int radius) pure nothrow @safe @nogc
    {
        this.radius = radius;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ChunkRadiusUpdated ret = new ChunkRadiusUpdated();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ChunkRadiusUpdated(radius: " ~ std.conv.to!string(this.radius) ~ ")";
    }

}

class ItemFrameDropItem : Pocket113Packet
{

    enum ubyte ID = 71;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["position", "item"];

    soupply.protocol.pocket113.types.BlockPosition position;
    soupply.protocol.pocket113.types.Slot item;

    this() pure nothrow @safe @nogc {}

    this(soupply.protocol.pocket113.types.BlockPosition position, soupply.protocol.pocket113.types.Slot item=soupply.protocol.pocket113.types.Slot.init) pure nothrow @safe @nogc
    {
        this.position = position;
        this.item = item;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ItemFrameDropItem ret = new ItemFrameDropItem();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ItemFrameDropItem(position: " ~ std.conv.to!string(this.position) ~ ", item: " ~ std.conv.to!string(this.item) ~ ")";
    }

}

class ReplaceItemInSlot : Pocket113Packet
{

    enum ubyte ID = 72;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["item"];

    soupply.protocol.pocket113.types.Slot item;

    this() pure nothrow @safe @nogc {}

    this(soupply.protocol.pocket113.types.Slot item) pure nothrow @safe @nogc
    {
        this.item = item;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ReplaceItemInSlot ret = new ReplaceItemInSlot();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ReplaceItemInSlot(item: " ~ std.conv.to!string(this.item) ~ ")";
    }

}

class GameRulesChanged : Pocket113Packet
{

    enum ubyte ID = 73;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["rules"];

    @Length!uint soupply.protocol.pocket113.types.Rule[] rules;

    this() pure nothrow @safe @nogc {}

    this(soupply.protocol.pocket113.types.Rule[] rules) pure nothrow @safe @nogc
    {
        this.rules = rules;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        GameRulesChanged ret = new GameRulesChanged();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "GameRulesChanged(rules: " ~ std.conv.to!string(this.rules) ~ ")";
    }

}

class Camera : Pocket113Packet
{

    enum ubyte ID = 74;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["unknown0", "unknown1"];

    @Var long unknown0;
    @Var long unknown1;

    this() pure nothrow @safe @nogc {}

    this(long unknown0, long unknown1=long.init) pure nothrow @safe @nogc
    {
        this.unknown0 = unknown0;
        this.unknown1 = unknown1;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        Camera ret = new Camera();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "Camera(unknown0: " ~ std.conv.to!string(this.unknown0) ~ ", unknown1: " ~ std.conv.to!string(this.unknown1) ~ ")";
    }

}

class AddItem : Pocket113Packet
{

    enum ubyte ID = 75;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["item"];

    soupply.protocol.pocket113.types.Slot item;

    this() pure nothrow @safe @nogc {}

    this(soupply.protocol.pocket113.types.Slot item) pure nothrow @safe @nogc
    {
        this.item = item;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        AddItem ret = new AddItem();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "AddItem(item: " ~ std.conv.to!string(this.item) ~ ")";
    }

}

class BossEvent : Pocket113Packet
{

    enum ubyte ID = 76;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    // event id
    enum uint ADD = 0;
    enum uint UPDATE = 1;
    enum uint REMOVE = 2;

    enum string[] __fields = ["entityId", "eventId"];

    @Var long entityId;
    @Var uint eventId;

    this() pure nothrow @safe @nogc {}

    this(long entityId, uint eventId=uint.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.eventId = eventId;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        BossEvent ret = new BossEvent();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "BossEvent(entityId: " ~ std.conv.to!string(this.entityId) ~ ", eventId: " ~ std.conv.to!string(this.eventId) ~ ")";
    }

}

class ShowCredits : Pocket113Packet
{

    enum ubyte ID = 77;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    // status
    enum int START = 0;
    enum int END = 1;

    enum string[] __fields = ["entityId", "status"];

    @Var long entityId;
    @Var int status;

    this() pure nothrow @safe @nogc {}

    this(long entityId, int status=int.init) pure nothrow @safe @nogc
    {
        this.entityId = entityId;
        this.status = status;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ShowCredits ret = new ShowCredits();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ShowCredits(entityId: " ~ std.conv.to!string(this.entityId) ~ ", status: " ~ std.conv.to!string(this.status) ~ ")";
    }

}

class AvailableCommands : Pocket113Packet
{

    enum ubyte ID = 78;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["commands", "unknown1"];

    string commands;
    string unknown1;

    this() pure nothrow @safe @nogc {}

    this(string commands, string unknown1=string.init) pure nothrow @safe @nogc
    {
        this.commands = commands;
        this.unknown1 = unknown1;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        AvailableCommands ret = new AvailableCommands();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "AvailableCommands(commands: " ~ std.conv.to!string(this.commands) ~ ", unknown1: " ~ std.conv.to!string(this.unknown1) ~ ")";
    }

}

class CommandStep : Pocket113Packet
{

    enum ubyte ID = 79;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["command", "overload", "unknown2", "currentStep", "done", "clientId", "input", "output"];

    string command;
    string overload;
    @Var uint unknown2;
    @Var uint currentStep;
    bool done;
    @Var ulong clientId;
    string input;
    string output;

    this() pure nothrow @safe @nogc {}

    this(string command, string overload=string.init, uint unknown2=uint.init, uint currentStep=uint.init, bool done=bool.init, ulong clientId=ulong.init, string input=string.init, string output=string.init) pure nothrow @safe @nogc
    {
        this.command = command;
        this.overload = overload;
        this.unknown2 = unknown2;
        this.currentStep = currentStep;
        this.done = done;
        this.clientId = clientId;
        this.input = input;
        this.output = output;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        CommandStep ret = new CommandStep();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "CommandStep(command: " ~ std.conv.to!string(this.command) ~ ", overload: " ~ std.conv.to!string(this.overload) ~ ", unknown2: " ~ std.conv.to!string(this.unknown2) ~ ", currentStep: " ~ std.conv.to!string(this.currentStep) ~ ", done: " ~ std.conv.to!string(this.done) ~ ", clientId: " ~ std.conv.to!string(this.clientId) ~ ", input: " ~ std.conv.to!string(this.input) ~ ", output: " ~ std.conv.to!string(this.output) ~ ")";
    }

}

class CommandBlockUpdate : Pocket113Packet
{

    enum ubyte ID = 80;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["updateBlock", "position", "mode", "redstoneMode", "conditional", "minecart", "command", "lastOutput", "hover", "trackOutput"];

    bool updateBlock;
    @Condition("updateBlock==true") soupply.protocol.pocket113.types.BlockPosition position;
    @Condition("updateBlock==true") @Var uint mode;
    @Condition("updateBlock==true") bool redstoneMode;
    @Condition("updateBlock==true") bool conditional;
    @Condition("updateBlock==false") @Var long minecart;
    string command;
    string lastOutput;
    string hover;
    bool trackOutput;

    this() pure nothrow @safe @nogc {}

    this(bool updateBlock, soupply.protocol.pocket113.types.BlockPosition position=soupply.protocol.pocket113.types.BlockPosition.init, uint mode=uint.init, bool redstoneMode=bool.init, bool conditional=bool.init, long minecart=long.init, string command=string.init, string lastOutput=string.init, string hover=string.init, bool trackOutput=bool.init) pure nothrow @safe @nogc
    {
        this.updateBlock = updateBlock;
        this.position = position;
        this.mode = mode;
        this.redstoneMode = redstoneMode;
        this.conditional = conditional;
        this.minecart = minecart;
        this.command = command;
        this.lastOutput = lastOutput;
        this.hover = hover;
        this.trackOutput = trackOutput;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        CommandBlockUpdate ret = new CommandBlockUpdate();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "CommandBlockUpdate(updateBlock: " ~ std.conv.to!string(this.updateBlock) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", mode: " ~ std.conv.to!string(this.mode) ~ ", redstoneMode: " ~ std.conv.to!string(this.redstoneMode) ~ ", conditional: " ~ std.conv.to!string(this.conditional) ~ ", minecart: " ~ std.conv.to!string(this.minecart) ~ ", command: " ~ std.conv.to!string(this.command) ~ ", lastOutput: " ~ std.conv.to!string(this.lastOutput) ~ ", hover: " ~ std.conv.to!string(this.hover) ~ ", trackOutput: " ~ std.conv.to!string(this.trackOutput) ~ ")";
    }

}

class UpdateTrade : Pocket113Packet
{

    enum ubyte ID = 81;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["window", "windowType", "unknown2", "unknown3", "willing", "trader", "player", "displayName", "offers"];

    ubyte window;
    ubyte windowType = 15;
    @Var int unknown2;
    @Var int unknown3;
    bool willing;
    @Var long trader;
    @Var long player;
    string displayName;
    @Bytes ubyte[] offers;

    this() pure nothrow @safe @nogc {}

    this(ubyte window, ubyte windowType=15, int unknown2=int.init, int unknown3=int.init, bool willing=bool.init, long trader=long.init, long player=long.init, string displayName=string.init, ubyte[] offers=(ubyte[]).init) pure nothrow @safe @nogc
    {
        this.window = window;
        this.windowType = windowType;
        this.unknown2 = unknown2;
        this.unknown3 = unknown3;
        this.willing = willing;
        this.trader = trader;
        this.player = player;
        this.displayName = displayName;
        this.offers = offers;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        UpdateTrade ret = new UpdateTrade();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "UpdateTrade(window: " ~ std.conv.to!string(this.window) ~ ", windowType: " ~ std.conv.to!string(this.windowType) ~ ", unknown2: " ~ std.conv.to!string(this.unknown2) ~ ", unknown3: " ~ std.conv.to!string(this.unknown3) ~ ", willing: " ~ std.conv.to!string(this.willing) ~ ", trader: " ~ std.conv.to!string(this.trader) ~ ", player: " ~ std.conv.to!string(this.player) ~ ", displayName: " ~ std.conv.to!string(this.displayName) ~ ", offers: " ~ std.conv.to!string(this.offers) ~ ")";
    }

}

class UpdateEquip : Pocket113Packet
{

    enum ubyte ID = 82;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = [];

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        UpdateEquip ret = new UpdateEquip();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "UpdateEquip()";
    }

}

class ResourcePackDataInfo : Pocket113Packet
{

    enum ubyte ID = 83;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["id", "maxChunkSize", "chunkCount", "compressedPackSize", "sha256"];

    string id;
    @LittleEndian uint maxChunkSize;
    @LittleEndian uint chunkCount;
    @LittleEndian ulong compressedPackSize;
    string sha256;

    this() pure nothrow @safe @nogc {}

    this(string id, uint maxChunkSize=uint.init, uint chunkCount=uint.init, ulong compressedPackSize=ulong.init, string sha256=string.init) pure nothrow @safe @nogc
    {
        this.id = id;
        this.maxChunkSize = maxChunkSize;
        this.chunkCount = chunkCount;
        this.compressedPackSize = compressedPackSize;
        this.sha256 = sha256;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ResourcePackDataInfo ret = new ResourcePackDataInfo();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ResourcePackDataInfo(id: " ~ std.conv.to!string(this.id) ~ ", maxChunkSize: " ~ std.conv.to!string(this.maxChunkSize) ~ ", chunkCount: " ~ std.conv.to!string(this.chunkCount) ~ ", compressedPackSize: " ~ std.conv.to!string(this.compressedPackSize) ~ ", sha256: " ~ std.conv.to!string(this.sha256) ~ ")";
    }

}

class ResourcePackChunkData : Pocket113Packet
{

    enum ubyte ID = 84;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["id", "chunkIndex", "progress", "data"];

    string id;
    @LittleEndian uint chunkIndex;
    @LittleEndian ulong progress;
    @EndianLength!uint(Endian.littleEndian) ubyte[] data;

    this() pure nothrow @safe @nogc {}

    this(string id, uint chunkIndex=uint.init, ulong progress=ulong.init, ubyte[] data=(ubyte[]).init) pure nothrow @safe @nogc
    {
        this.id = id;
        this.chunkIndex = chunkIndex;
        this.progress = progress;
        this.data = data;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ResourcePackChunkData ret = new ResourcePackChunkData();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ResourcePackChunkData(id: " ~ std.conv.to!string(this.id) ~ ", chunkIndex: " ~ std.conv.to!string(this.chunkIndex) ~ ", progress: " ~ std.conv.to!string(this.progress) ~ ", data: " ~ std.conv.to!string(this.data) ~ ")";
    }

}

class ResourcePackChunkRequest : Pocket113Packet
{

    enum ubyte ID = 85;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = ["id", "chunkIndex"];

    string id;
    @LittleEndian uint chunkIndex;

    this() pure nothrow @safe @nogc {}

    this(string id, uint chunkIndex=uint.init) pure nothrow @safe @nogc
    {
        this.id = id;
        this.chunkIndex = chunkIndex;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ResourcePackChunkRequest ret = new ResourcePackChunkRequest();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ResourcePackChunkRequest(id: " ~ std.conv.to!string(this.id) ~ ", chunkIndex: " ~ std.conv.to!string(this.chunkIndex) ~ ")";
    }

}

class Transfer : Pocket113Packet
{

    enum ubyte ID = 86;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["ip", "port"];

    string ip;
    @LittleEndian ushort port = 19132;

    this() pure nothrow @safe @nogc {}

    this(string ip, ushort port=19132) pure nothrow @safe @nogc
    {
        this.ip = ip;
        this.port = port;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        Transfer ret = new Transfer();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "Transfer(ip: " ~ std.conv.to!string(this.ip) ~ ", port: " ~ std.conv.to!string(this.port) ~ ")";
    }

}

class PlaySound : Pocket113Packet
{

    enum ubyte ID = 87;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["name", "position", "volume", "pitch"];

    string name;
    soupply.protocol.pocket113.types.BlockPosition position;
    float volume;
    float pitch;

    this() pure nothrow @safe @nogc {}

    this(string name, soupply.protocol.pocket113.types.BlockPosition position=soupply.protocol.pocket113.types.BlockPosition.init, float volume=float.init, float pitch=float.init) pure nothrow @safe @nogc
    {
        this.name = name;
        this.position = position;
        this.volume = volume;
        this.pitch = pitch;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        PlaySound ret = new PlaySound();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "PlaySound(name: " ~ std.conv.to!string(this.name) ~ ", position: " ~ std.conv.to!string(this.position) ~ ", volume: " ~ std.conv.to!string(this.volume) ~ ", pitch: " ~ std.conv.to!string(this.pitch) ~ ")";
    }

}

class StopSound : Pocket113Packet
{

    enum ubyte ID = 88;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = ["name", "stopAll"];

    string name;
    bool stopAll;

    this() pure nothrow @safe @nogc {}

    this(string name, bool stopAll=bool.init) pure nothrow @safe @nogc
    {
        this.name = name;
        this.stopAll = stopAll;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        StopSound ret = new StopSound();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "StopSound(name: " ~ std.conv.to!string(this.name) ~ ", stopAll: " ~ std.conv.to!string(this.stopAll) ~ ")";
    }

}

class SetTitle : Pocket113Packet
{

    enum ubyte ID = 89;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    // action
    enum int HIDE = 0;
    enum int RESET = 1;
    enum int SET_TITLE = 2;
    enum int SET_SUBTITLE = 3;
    enum int SET_ACTION_BAR = 4;
    enum int SET_TIMINGS = 5;

    enum string[] __fields = ["action", "text", "fadeIn", "stay", "fadeOut"];

    @Var int action;
    string text;
    @Var int fadeIn;
    @Var int stay;
    @Var int fadeOut;

    this() pure nothrow @safe @nogc {}

    this(int action, string text=string.init, int fadeIn=int.init, int stay=int.init, int fadeOut=int.init) pure nothrow @safe @nogc
    {
        this.action = action;
        this.text = text;
        this.fadeIn = fadeIn;
        this.stay = stay;
        this.fadeOut = fadeOut;
    }

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        SetTitle ret = new SetTitle();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "SetTitle(action: " ~ std.conv.to!string(this.action) ~ ", text: " ~ std.conv.to!string(this.text) ~ ", fadeIn: " ~ std.conv.to!string(this.fadeIn) ~ ", stay: " ~ std.conv.to!string(this.stay) ~ ", fadeOut: " ~ std.conv.to!string(this.fadeOut) ~ ")";
    }

}

class AddBehaviorTree : Pocket113Packet
{

    enum ubyte ID = 90;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = [];

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        AddBehaviorTree ret = new AddBehaviorTree();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "AddBehaviorTree()";
    }

}

class StructureBlockUpdate : Pocket113Packet
{

    enum ubyte ID = 91;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = [];

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        StructureBlockUpdate ret = new StructureBlockUpdate();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "StructureBlockUpdate()";
    }

}

class ShowStoreOffer : Pocket113Packet
{

    enum ubyte ID = 92;

    enum bool CLIENTBOUND = true;
    enum bool SERVERBOUND = false;

    enum string[] __fields = [];

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        ShowStoreOffer ret = new ShowStoreOffer();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "ShowStoreOffer()";
    }

}

class PurchaseReceipt : Pocket113Packet
{

    enum ubyte ID = 93;

    enum bool CLIENTBOUND = false;
    enum bool SERVERBOUND = true;

    enum string[] __fields = [];

    mixin Make;

    public static typeof(this) fromBuffer(ubyte[] buffer)
    {
        PurchaseReceipt ret = new PurchaseReceipt();
        ret.decode(buffer);
        return ret;
    }

    public override string toString()
    {
        return "PurchaseReceipt()";
    }

}
